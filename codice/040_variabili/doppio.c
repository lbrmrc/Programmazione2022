#include <stdio.h>

main() {
    int v; // definizione della variabile v di tipo intero
    v = 4; // assegnamento a v del valore 4
    // stampa del doppio di v
    printf("%d\n", 2 * v); // v è una espressione variabile
}